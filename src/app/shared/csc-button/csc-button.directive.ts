import { Directive } from '@angular/core';

/**
 *
 * Directive whose purpose is to add the
 * csc buttons base styles to the host
 *
 * @export
 * @class CscButtonDirective
 */
@Directive({
  selector: '[csc-button]'
})
export class CscButtonDirective {}
