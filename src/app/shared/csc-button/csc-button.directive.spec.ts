import { CscButtonDirective } from './csc-button.directive';

describe('CscButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new CscButtonDirective();
    expect(directive).toBeTruthy();
  });
});
